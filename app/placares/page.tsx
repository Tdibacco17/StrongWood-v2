import styles from './page.module.scss'

export default function ClosetsPage() {
    return (
        <section className={styles['container-section-closets']}>
            closets
        </section>
    )
}